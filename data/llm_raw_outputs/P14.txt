{
  "paper_id": "P14",
  "repo_name": "pymc-devs/pymc",
  "env": {
    "type": "conda",
    "setup_commands": [
      "micromamba create -n pymc python=3.9 -y",
      "micromamba install -n pymc pip -y",
      "micromamba run -n pymc pip install setuptools<60.0.0 wheel<0.37.0 -y"
    ]
  },
  "steps": [
    {
      "id": "install_dependencies",
      "description": "Installing dependencies",
      "working_dir": ".",
      "commands": [
        "micromamba run -n pymc pip install -r requirements.txt"
      ],
      "expected_artifacts": ["pymc"]
    },
    {
      "id": "run_main_experiment",
      "description": "Running the main experiment",
      "working_dir": ".",
      "commands": [
        "micromamba run -n pymc python tests/models.py"
      ],
      "expected_artifacts": ["output.txt"]
    }
  ]
}
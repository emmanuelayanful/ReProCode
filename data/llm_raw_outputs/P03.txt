{
  "paper_id": "P03",
  "repo_name": "google-research/google-research",
  "env": {
    "type": "conda",
    "setup_commands": [
      "micromamba create -n gr_env python=3.9 -y",
      "micromamba install -n gr_env pip -y",
      "micromamba run -n gr_env pip install setuptools<60.0.0 wheel<0.37.0"
    ]
  },
  "steps": [
    {
      "id": "install_dependencies",
      "description": "Install dependencies",
      "working_dir": ".",
      "commands": [
        "micromamba run -n gr_env pip install -r speculative_kd/requirements.txt",
        "micromamba run -n gr_env pip install -r action_gap_rl/requirements.txt",
        "micromamba run -n gr_env pip install -r multi_annotator/requirements.txt",
        "micromamba run -n gr_env pip install -r camp_zipnerf/requirements.txt"
      ],
      "expected_artifacts": ["speculative_kd", "action_gap_rl", "multi_annotator", "camp_zipnerf"]
    },
    {
      "id": "download_data",
      "description": "Prepare/download data",
      "working_dir": ".",
      "commands": [
        "git lfs install",
        "git lfs fetch --all"
      ],
      "expected_artifacts": ["data files"]
    },
    {
      "id": "run_experiment",
      "description": "Run the main experiment",
      "working_dir": "speculative_kd",
      "commands": [
        "./run.sh"
      ],
      "expected_artifacts": ["experiment results"]
    }
  ]
}
```json
{
    "paper_id": "P04",
    "repo_name": "swe-bench/swe-bench",
    "env": {
        "type": "conda",
        "setup_commands": [
            "conda create -n swebench python=3.8",
            "conda activate swebench",
            "pip install -e .",
            "pip install docker"
        ]
    },
    "steps": [
        {
            "id": "Install Dependencies",
            "description": "Install required packages using Conda",
            "working_dir": "/path/to/repo",
            "commands": [
                "conda env update",
                "pip install -r requirements.txt"
            ],
            "expected_artifacts": []
        },
        {
            "id": "Download Data (Optional)",
            "description": "If data is not included, skip this step",
            "working_dir": "/path/to/repo",
            "commands": [
                "python scripts/download_data.py"  # Assuming a script exists for data download
            ],
            "expected_artifacts": ["data.zip"]  # Assuming the downloaded data is zipped
        },
        {
            "id": "Run Main Experiment",
            "description": "Evaluate patch predictions using the evaluation harness",
            "working_dir": "/path/to/repo",
            "commands": [
                "python -m swebench.harness.run_evaluation --predictions_path <path_to_predictions> --max_workers <num_workers> --run_id <run_id>",
                # Replace `<path_to_predictions>`, `<num_workers>`, and `<run_id>` with actual values
            ],
            "expected_artifacts": ["evaluation_results"]
        }
    ]
}
```
```json
{
    "paper_id": "P07",
    "repo_name": "code-rag-bench/code-rag-bench",
    "env": {
        "type": "conda",
        "setup_commands": [
            "conda create -n crag python=3.10 -y",
            "conda activate crag",
            "pip install -r requirements.txt"
        ]
    },
    "steps": [
        {
            "id": "Retrieval Setup",
            "description": "Install and set up retrieval components",
            "working_dir": "retrieval",
            "commands": [
                "python -m create/${data_name}.py" if "data_name" in locals() else "python create/humaneval.py" (assuming 'humaneval' dataset),
                "python generate_embeddings.py",
                "python3 eval_beir_sbert.py",
                "python3 eval_api_repo.py"
            ],
            "expected_artifacts": [
                "retrieval/datasets/{data_name}/corpus.jsonl",
                "retrieval/datasets/{data_name}/queries.jsonl",
                "retrieval/results/{dataset}_{model}_results.json" (if applicable)
            ]
        },
        {
            "id": "Generation Setup",
            "description": "Run code generation with a model",
            "working_dir": "generation",
            "commands": [
                "python main.py" (with appropriate task, model, and dataset path)
            ],
            "expected_artifacts": [
                "generation/outputs/model_name.json" (if generation is successful)
            ]
        }
    ]
}
```